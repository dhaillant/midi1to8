manage.py is a project in development.
The goal is to offer an interface for  setup and configure the behavior of the MIDI 1-to-8 module.

# How to setup:
Ubuntu:
```sudo apt install python3 python3-pyqt5 python3-mido python3-rtmidi```

How to execute:
```python3 manage.py```
